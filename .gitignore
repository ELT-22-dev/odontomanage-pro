# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
.vite-out
coverage
*.local
.env

# Supabase CLI local link cache (project ref, org id, pooler url — machine-local, not secret)
supabase/.temp/

# git-lfs on Windows sometimes mis-resolves its null-device hook path and
# writes hook files here instead of discarding them — harmless, but noisy.
/dev/

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
